{
    "name": "foundry",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "compile": "forge compile",
        "lint": "forge fmt",
        "test": "forge test",
        "dev": "forge compile",
        "chain": "source .env && anvil -m \"$MNEMONIC\"",
        "deploy": "source .env && forge script script/Deploy.s.sol --rpc-url http://localhost:8545 --broadcast"
    },
    "devDependencies": {},
    "dependencies": {}
}